{
  "name": "dhakker-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project dhakker-rules-test 'node --test rules.test.mjs'"
  },
  "//": [
    "Versions are pinned EXACTLY and must stay mutually compatible:",
    "@firebase/rules-unit-testing@5.0.1 declares peer firebase@^12.0.0.",
    "Mismatching these produces an npm ERESOLVE failure in CI. Do not fix",
    "such a failure with --force or --legacy-peer-deps; pick a compatible",
    "pair instead (4.0.1 pairs with firebase 11.x, 3.0.4 with firebase 10.x).",
    "package-lock.json is committed and CI installs with `npm ci`."
  ],
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.17.1",
    "firebase-tools": "13.35.1"
  }
}
